upstream orchestrator {
    server orchestrator1:5050 max_fails=3 fail_timeout=30s;
}

server {
    listen 8080;
    include /etc/nginx/mime.types;
    resolver 127.0.0.1 valid=5s;
    location / {
        proxy_pass http://orchestrator/;
    }
}